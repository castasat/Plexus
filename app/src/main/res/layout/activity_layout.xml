<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.button.MaterialButton
        android:id="@+id/advertiseButton"
        style="?attr/materialButtonOutlinedStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/advertise"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/discoverButton"
        style="?attr/materialButtonOutlinedStyle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/discover"
        app:layout_constraintEnd_toEndOf="@id/advertiseButton"
        app:layout_constraintStart_toStartOf="@id/advertiseButton"
        app:layout_constraintTop_toBottomOf="@+id/advertiseButton" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/sendButton"
        style="?attr/materialButtonOutlinedStyle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/send"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="@id/advertiseButton"
        app:layout_constraintStart_toStartOf="@id/advertiseButton"
        app:layout_constraintTop_toBottomOf="@+id/discoverButton" />

</androidx.constraintlayout.widget.ConstraintLayout>